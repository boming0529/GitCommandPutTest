<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="survey_extend.list">
      <field name="name">survey_extend list</field>
      <field name="model">survey_extend.survey_extend</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="survey_extend.action_window">
      <field name="name">survey_extend window</field>
      <field name="res_model">survey_extend.survey_extend</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="survey_extend.action_server">
      <field name="name">survey_extend server</field>
      <field name="model_id" ref="model_survey_extend_survey_extend"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="survey_extend" id="survey_extend.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="survey_extend.menu_1" parent="survey_extend.menu_root"/>
    <menuitem name="Menu 2" id="survey_extend.menu_2" parent="survey_extend.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="survey_extend.menu_1_list" parent="survey_extend.menu_1"
              action="survey_extend.action_window"/>
    <menuitem name="Server to list" id="survey_extend" parent="survey_extend.menu_2"
              action="survey_extend.action_server"/>
    -->
  </data>
</openerp>


            
